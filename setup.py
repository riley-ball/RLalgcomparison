from setuptools import setup

setup(name='twenty48stoch',
      version='0.0.1',
      install_requires=['gym'])

setup(name='twenty48determ',
      version='0.0.1',
      install_requires=['gym'])